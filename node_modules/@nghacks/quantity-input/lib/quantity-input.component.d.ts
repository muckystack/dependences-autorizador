import { EventEmitter, OnDestroy } from '@angular/core';
import { ControlValueAccessor, FormControl } from '@angular/forms';
export declare class QuantityInputComponent implements ControlValueAccessor, OnDestroy {
    quantityCtrl: FormControl;
    color: string;
    limit: number;
    private set limitValue(value);
    disabled: boolean;
    private set disabledValue(value);
    private _unsubscribeAll;
    quantityChange: EventEmitter<number>;
    onChange: (quantity: number) => void;
    onTouched: () => void;
    get value(): number;
    constructor();
    increase(): void;
    decrease(): void;
    writeValue(quantity: number): void;
    registerOnChange(fn: (quantity: number) => void): void;
    registerOnTouched(fn: () => void): void;
    setDisabledState(isDisabled: boolean): void;
    ngOnDestroy(): void;
}
